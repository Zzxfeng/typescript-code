let isDone: boolean = true; //布尔类型限定
let num: number = 6; //数字类型限定
let str: string = "string"; //字符串
let arr: number[] = [1, 2, 5, 8]; //数组（项为纯数字）
let arr1: Array<number> = [1, 2, 3, 8]; //数组（项为纯数字）
let arr2: [number, string] = [2, "str"]; //元组（允许表示一个已知元素数量和类型的数组）
let arr3: number[][] = [[1,2,3],[1,2,3],[1,2,3]] //多重数组
